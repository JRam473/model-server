services:
  - type: web
    name: modelo-server
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python modelo_server.py
    envVars:
      - key: PORT
        value: 10000